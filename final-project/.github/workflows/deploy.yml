name: Final Project CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ~/devops-practice/final-project
            git pull origin main
            docker stop final-app || true
            docker rm final-app || true
            docker build -t final-app .
            docker run -d \
              -p 8888:80 \
              --name final-app \
              --restart always \
              final-app
            echo "✅ Final project deployed!"
```

### Шаг 4 — Открой порт 8888 в Oracle Cloud

Так же как открывал другие порты — добавь правило:
```
Protocol: TCP
Port: 8888
Source: 0.0.0.0/0
